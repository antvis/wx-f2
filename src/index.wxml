<view class="container">
  <canvas
    wx:if="{{!dataURL}}"  
    type="2d"
    class="f2-canvas"
    bindtouchstart="dispatch"
    bindtouchmove="dispatch"
    bindtouchend="dispatch"
  />
  <image wx:if="{{dataURL}}" src="{{dataURL}}"/>
</view>